# hero-video

HyperFrames source for the landing-page hero demo video (site/assets/hero-demo.mp4).

```sh
npx hyperframes preview   # edit in Studio
npx hyperframes render --quality high --output ../site/assets/hero-demo.mp4
```
